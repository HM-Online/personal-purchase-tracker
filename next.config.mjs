/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your Next.js configuration options can go here if needed in the future.
  // An empty object is perfectly fine for now.
};

export default nextConfig;